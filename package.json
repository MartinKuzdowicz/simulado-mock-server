{
  "name": "simulado-mock-server",
  "version": "1.0.0",
  "description": "simulado mock server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "win-start": "cd node_modules/.bin && simulado --file ..\\..\\mock-endpoints.json && cd ..\\..\\",
    "start": "./node_modules/simulado/bin/simulado --file ./mock-endpoints.json"
  },
  "author": "kuzdowicz",
  "license": "ISC",
  "dependencies": {
    "simulado": "^2.8.0"
  }
}
